apiVersion: monitoring.coreos.com/v1 #Báo cho Prometheus biết đường đi lấy metrics
kind: ServiceMonitor
metadata:
  name: api-monitor
  namespace: demo
spec:
  selector: { matchLabels: { app: api } }
  endpoints: [ { port: http, path: /metrics, interval: 15s } ]